# 実装状況 — 仕様書 v0.8 との対応

全体: 128ファイル / 約39,800行(ヘッダ+実装、`Source/`配下)。

**ビルド状況(2026-08-29更新)**: Visual Studio Build Tools 2022 (MSVC 14.44) + CMake 4.4.2 でDebug/Releaseとも**ビルド成功・エラー0**、ユニットテスト**2,694件全通過**(`--run-tests`で直接再確認済み)。起動してウィンドウが描画され、オーディオデバイス(WASAPI 48kHz / buffer 480 / 30.0ms)が開くところまで確認済み。UI文字列の未翻訳ゼロ。契約ヘッダのギャップは16点すべて解消。2026-08-26時点からの4日間で、UTAU連携Phase1・DAWproject相互運用・拡張プラグインAPI(フォーマット変換)・ドッキングレイアウトシステム・セッションビューを実装し、テストは1,903件→2,694件に増えた。詳細は下記「仕様書外の追加実装」を参照。

**まだ確認していないこと**: 実際に音が出るか、録音できるか、採譜・生成が最後まで走るか。プラグインのランタイム隔離(ギャップ16)は配線の往復自体は実プロセスで確認したが、実プラグインでの音出し・エディタ表示はスキャンが通る環境での確認待ち(下記参照)。UIは起動時のタイムライン画面以外を目視していない。拡張プラグインAPI・UTAU連携・DAWproject相互運用はいずれも自動テストのみで、実際の外部プロセス/実ボイスバンク/実DAWとの往復確認はまだ。

---

## P0 — Must-Have(仕様書11章)

| 項目 | 状態 | 備考 |
|---|---|---|
| 音声→モノフォニックMIDI変換 | 実装 | YIN + オンセット検出 + ノート分割。信頼度は実測値から導出 |
| ステム分離 | **外部依存** | Demucs互換の実行ファイルを設定で指定する方式。モデルは同梱しない。未設定でも採譜自体は動く |
| 基本的なポリフォニック変換 | 実装(精度に天井あり) | ハーモニックサム。信頼度に0.60を掛けてUI上で薄く表示 |
| MIDI+コードからの自動アレンジ生成 | 実装 | 10ジャンル × 8パートのテンプレート + シード固定の制約付きランダムウォーク |
| ハーモナイズ・ヒューマナイズ | 実装 | ヒューマナイズは拍子対応済み(6/8は複合拍子として処理) |
| ピアノロール編集 | 実装 | 描画/移動/リサイズ/ベロシティ/スケールハイライト/信頼度シェーディング |
| MIDI/オーディオ/譜面の書き出し | 実装(制限あり) | 音声書き出しはミックスダウンのみ。ステム別書き出しはエンジン側に実装があるがUI未接続 |
| プロジェクト保存・再読み込み | 実装 | `.ssproj` JSON。temp+swapで書き込み中クラッシュから前回保存を保護 |
| 信頼度の可視化・低信頼度ジャンプ | 実装 | 色覚特性対応(明度+彩度のランプ + ハッチング併用) |
| オーディオI/F経由の録音 | 実装 | マルチチャンネル、ロックフリー、ドロップ検出、入力モニタリング。**MIDI録音は未実装** |
| 波形編集(カット/フェード/ゲイン/ノーマライズ) | 実装 | 非破壊・Undo対応。ノーマライズのピークはサムネイル由来で近似 |
| VST3/AUの読み込みとサンドボックス安定動作 | 実装 | スキャン・再生中のホスティングとも子プロセス隔離(2026-08-26、下記ギャップ16解消) |
| サウンドフォント/サンプル取り込み | **未実装** | .sf2/SFZ ローダーが無い。当面の代替として「ScoreSmith Basic Synth」(sine/saw/square+ADSRの簡易内蔵シンセ)をミキサーの「Add instrument→Built-in instruments」に追加(2026-08-26)。本格的なサンプル再生の代替にはならない |
| 基本エフェクト + マスターメーター | 実装 | EQ/コンプ/ゲート/リミッター/サチュレーター/リバーブ/ディレイ/ラウドネスメーター(BS.1770準拠) |

**P0で欠けているもの: サウンドフォント取り込み、MIDI録音。**

## P1 — Should-Have

| 項目 | 状態 |
|---|---|
| 続き生成・スタイル変換 | 実装(スタイル変換は限界を候補名に明示) |
| バッチ処理 | 未実装 |
| MIDIコントローラー対応 | 部分(MIDI入力は受けるが MIDI Learn 未実装) |
| ジャンルプリセット | 実装(10ジャンル) |
| 候補ギャラリーUI | 実装 |
| マルチチャンネル同時録音 | 実装 |
| テイク管理・コンピング | 未実装 |
| 拡張ミキサー(バス/センド/オートメーション) | 実装(バス→バスのルーティングは非対応) |
| タイムストレッチ・ピッチシフト | 未実装(`playbackRate` フィールドのみ存在) |
| ラウドネスプリセット書き出し | 未実装(メーター自体は実装済み) |
| テンポマップ・コードトラック | 実装(データモデル + MusicXML出力。専用編集UIは無し) |
| 五線譜での基本編集 | 読み取り専用(Phase 2) |
| ハイレゾ対応 44.1k〜384kHz / 32-bit float | 実装 |

## P2 / Phase 3+

映像同期(9.9)、サラウンド(8.4.10)、モジュラー(9.11)はプレースホルダ画面のみ。**セッションビュー(9.10、track×sceneのクリップランチャー)は2026-08-27に実装済み**(下記参照)。プラグイン版・クラウド同期・モバイルは未着手。

---

## 仕様書外の追加実装(2026-08-26〜2026-08-29)

仕様書v0.8には無いが、実運用の要望・フィードバックを受けて追加した機能。いずれもユニットテストが緑であることは確認済みだが、実機での往復確認(外部拡張機能プロセス・実ボイスバンク・実DAWとの相互運用)は次回以降の課題。

| 機能 | 状態 | 概要 |
|---|---|---|
| セッションビュー(9.10) | 実装 | track × scene のクリップランチャー画面。仕様書ではPhase 3+扱いだったが前倒しで実装 |
| ドッキングレイアウトシステム | 実装 | パネルをタブとして自由にドラッグ&ドッキングできる汎用ドックコンテナ(`Source/UI/Dock/`)。10タスクのSDDで実装。デフォルト起動レイアウトでPiano Rollを非表示化(タブに格納、必要時に自分でドッキング) |
| DAWproject相互運用 | 実装 | Bitwig/Studio One/Cubase等が対応する[DAWproject](https://github.com/bitwig/dawproject)形式のフルフィデリティ import/export。トラック・チャンネル・バス・ルーティング・センド・テンポ・オートメーション・マーカー・シーン・プラグインstateまで往復対応 |
| 拡張プラグインAPI(フォーマット変換) | 実装 | サードパーティが `manifest.json` + 任意言語の実行ファイルで import/export フォーマットを追加できる仕組み。DAWprojectを中間形式として経由する外部プロセス方式。設定 > Extensions タブでスキャン対象フォルダを複数登録可能。作り方はアプリ内Help「拡張機能の作り方」とREADMEの両方に記載(友人からのフィードバック対応) |
| UTAU連携 Phase 1 | 実装 | `.ust`ファイルの読み込み→ボイスバンク(oto.ini)割り当て→外部リサンプラでのレンダリング→通常のオーディオクリップと同じ経路での再生、`.ssproj`への永続化まで。歌詞とエイリアスの直接一致のみ対応(VCV/CVVC前後接続の自動選択、`.ustx`、専用編集UIはPhase 2) |
| パン数値表示 | 実装 | ミキサーチャンネルストリップにパン値の数値表示オプションを追加 |
| パフォーマンス設定 | 実装 | 再描画レート・Undo履歴上限を設定画面から調整可能に |
| 出力デバイスのクイック切替 | 実装 | トランスポートバーから出力オーディオデバイスを素早く切り替え可能に |

---

## 契約ヘッダのギャップ — 16点すべて解消(2026-08-26)

当初4エージェントが独立に報告してきた16点、**全点を修正済み**。1〜15は3エージェント並列で、最後まで残っていたギャップ16(プラグインのランタイム別プロセス実行)は2エージェント並列で潰した。いずれも各自が自分のコピーでビルド+テスト緑を確認してから統合し、担当ファイルの重複はゼロ、統合ビルドは一発で成功している。

| # | 内容 | 対応 |
|---|---|---|
| 1 | `Mixer::process` に入力バッファが無い | 署名に `deviceInput` を追加。`Track::inputMonitoring` を新設し、録音待機中のトラックが自分のFXチェイン・フェーダー・パンを通してモニタリングできるように(仕様8.4.3) |
| 2 | `Mixer::process` に再生状態が無い | 署名に `isPlaying` を追加。プレイヘッド移動からの推測を廃止し、停止位置からの再生再開で最初の1ブロックを取りこぼす問題を解消 |
| 3 | `Project` にバス型が無い | `ss::Bus`(名前/ゲイン/パン/ミュート/内蔵FX)と `Track::Send` を新設。トラック→バス→マスターで実際にレンダリングし、ミキサーにバスストリップと `+ Bus` を追加 |
| 4 | `Mixer::setSolo` が無い | 追加。ソロが `rebuild()` を経由しなくなった。あわせてソロをミュートに畳む旧実装を廃止(バスがある以上、ソロ中のトラックはマスターでなく自分のバスに届く必要がある) |
| 5 | `Recorder::finish` にテンポマップが無い | 各テイクの長さを実際の書き込みサンプル数から算出。ブロックを落としたチャンネルは短いクリップになる |
| 6 | `Generator::Candidate` に自由記述欄が無い | `notes` を追加。スタイル変換の限界警告が候補名から外れ、ギャラリーに表示される |
| 7 | `Generator::humanise` に拍子が無い | 拍子を受け取り、6/8を複合拍子として検出して付点4分でパルスするように |
| 8 | `chordSymbolFor` に `preferFlats` が無い | 追加。`keyPrefersFlats()` を新設し、キーを持つ6箇所の呼び出しすべてで綴りを決定 |
| 9 | `Suggestion` が `Note` 1個 | N→M置換に変更。マージ提案が元の断片を残さなくなった |
| 10 | 進捗コールバックで中断できない | `std::function<bool(float)>` に変更。DSPのホットループ(64フレームごと)まで中断が届く |
| 11 | `masterChain` が `Project` に無い | `Project` へ移動。マスターFXが保存され、Undoも効くように |
| 12 | マスターにトゥルーピークが無い | `getMasterTruePeakDb()` を追加。LUFS / サンプルピーク / トゥルーピークを別々に表示 |
| 13 | プラグインbypassのセッターが無い | 追加。チェイン署名からbypassを外したのでトグルで再ロードしなくなった |
| 14 | `PluginSlot` にインストゥルメント判別が無い | `isInstrument` をモデルと永続化に追加。旧ファイルは従来の推測にフォールバック |
| 15 | オートメーションの編集UIが無い | タイムラインにレーンエディタを新設(ブレークポイントの追加/移動/削除、範囲選択、グリッドスナップ、Undo対応)。**あわせて音声スレッドがレーンを実際に適用するように** |

### 統合時に見つけた追加バグ

`topologyFingerprint` がオートメーションの `parameterId` と点の**個数**しか見ていなかったため、**点を移動しても音に反映されなかった**(個数が変わらないので再解決が走らない)。ブレークポイントの位置をfingerprintに含めて修正。エディタはマウスアップで1回コミットするので、コストは点の追加時と同じ。

### ギャップ16(プラグインのランタイム別プロセス実行) — 解消(2026-08-26)

仕様書10.3・15.6が「妥協しない」最優先事項と位置づけている項目。スキャン時の隔離(ChildProcessWorker/Coordinator、dead-man's-pedal、30秒タイムアウトでブラックリスト)に続き、**再生中のホスティングも子プロセス隔離に切り替えた。プラグインが再生中に落ちても、落ちるのはそのプラグイン専用のワーカープロセスだけで、DAW本体は生き続ける。**

構成:

- `Source/Plugins/PluginHostProtocol.h` — ワーカーとプロキシが共有する配線契約。`Cmd`列挙体(load/prepare/release/process/getState/setState/openEditor/resizeEditor/closeEditor/heartbeat/editorResized)と、両側が一字一句同じ関数を呼ぶ`read*`/`write*`ヘルパーで、バイト配置のズレが起きない設計
- `Source/Plugins/PluginHostWorker.cpp` — 子プロセス側。プラグインごとに1プロセス(同じプラグインを2トラックに挿しても2プロセス)。`load/prepare/release/getState/setState/openEditor/resizeEditor/closeEditor` はメッセージスレッドへ、`process`(1ブロックのオーディオ)は専用オーディオスレッドへ、`heartbeat` はパイプの受信コールバックその場で即答 — エディタがお絵描き中でも生死確認が偽陽性を出さない
- `Source/Plugins/PluginHostProxy.h/.cpp` — 親プロセス側。`juce::AudioPluginInstance`として振る舞う透過プロキシで、`Mixer.cpp`・`MixerView.cpp`は一切変更していない(既存コードは元々抽象インターフェース越しにしかプラグインを触っていなかったため)。ウォッチドッグはハートビート2回連続ミスまたは`handleConnectionLost`即時発火でクラッシュと判定し、直前の state で1回だけ自動再起動、それでも落ちれば以後は無音化して恒久停止(無限クラッシュループ防止)
- エディタのウィンドウ埋め込みはWindows専用(`SetParent`/`SetWindowLongPtr`によるHWND再親化)。Mac版は開発・検証機が無いため、NSView/XPCによる越境リモーティングは実装せず、ワーカー側で独立ウィンドウとして開く簡略化に留めている(コード内に`ponytail:`で明記)

**実機での限定確認**: このマシンに実在するVST3(Overloud TH3-64、Celemony Melodyne)へ向けて使い捨ての診断コードから`createAndLoad`を直接叩き、ワーカープロセスの起動→パイプ確立→`load`リクエスト送信→ワーカー内での`AudioPluginFormatManager::createPluginInstance`呼び出し→エラー応答の返送、という一連の経路が実プロセス境界を越えて動くことは確認した。ただし両プラグインとも `findAllTypesForFile` による列挙(既存のスキャン機能と同一のJUCE呼び出し)がこの環境では0件を返すため、正式なスキャン経由での実音出し・エディタ表示までは確認できていない — スキャンさえ通れば同じ経路に乗るはずだが、次のセッションでの実機確認が必要。

---

## 意図的な妥協点

コード内の `ponytail:` コメント41箇所に、天井と改善方法をその場に書いてある。

```
grep -rn "ponytail:" Source/
```

主なもの: 文書全体スナップショット方式のUndo(編集ごとにO(プロジェクトサイズ))、pYINのHMM無し、リニア周波数STFTによる低音域の分解能不足、ステム書き出しのNパス処理、カウントインがブロック精度。

---

## テスト

`juce::UnitTest` を14本、**全2,694件通過**(2026-08-29、`--run-tests`で再確認済み)。08-26時点の5本から、UTAU連携・DAWproject相互運用・拡張プラグインAPI・ドッキング・セッションビューの追加に伴い9本増えた。

| ファイル | 対象 |
|---|---|
| `Source/Engine/EngineTests.cpp` | 拍⇄サンプル往復(テンポ変化跨ぎ含む)、ループ折り返し、300ブロックのソーク、テイク長がテンポマップ経由で算出されること |
| `Source/Mixer/MixerTests.cpp` | 全8エフェクトのパラメータ往復・正規化書き込み、パン則、バス/センド/マスターFXの永続化往復、ストリップ→バス→マスターのブロック経路(モニタリング・ソロ・センド・自動化) |
| `Source/AI/AITests.cpp` | 既知周波数の正弦波でのYIN検出、コード記号の往復(フラット表記含む)、キー推定、シード再現性、3/4・6/8のアクセント格子、中断が実際に早く終わること |
| `Source/UI/AutomationEditor.cpp` | カーブ評価(空レーン・範囲外の保持・0幅ステップ)、点の挿入順序・削除・グリッドスナップ |
| `Source/Plugins/BasicSynth.cpp` | ノートオンで実際に音が出ること、ノートオフでリリーステール後に無音へ戻ること(鳴りっぱなし防止)、パラメータのstate往復 |
| `Source/Core/UtauTypesTests.cpp` | UtauNote/UtauClipのデータモデル、`.ssproj`への永続化往復(全フィールド網羅) |
| `Source/Vocal/VocalTests.cpp` | UtauRendererのオーケストレーション、エイリアス不一致時の警告付き継続、レンダリング結果の音声クリップ経路への再生連携 |
| `Source/IO/DawProjectTests.cpp` | DAWprojectのフルフィデリティ import/export往復(トラック/バス/センド/オートメーション/マーカー/シーン/プラグインstate)、壊れたアーカイブの警告付きスキップ |
| `Source/Extensions/FormatExtensionTests.cpp` | manifest.jsonパース、実行ファイルパスのフォルダ包含チェック(`isAChildOf`)、外部プロセス経由のexport/import、タイムアウト処理 |
| `Source/UI/Dock/DockTests.cpp` | ドックへのタブ追加/移動/削除、レイアウトの永続化往復 |
| `Source/UI/SessionViewTests.cpp` | track × sceneのクリップランチャー、シーン発火時の再生状態遷移 |
| `Source/UI/GenerateViewTests.cpp`, `Source/UI/UiSupportTests.cpp`, `Source/UI/WhatsNewDialogTests.cpp`, `Source/Core/ProjectTests.cpp`, `Source/IO/IoTests.cpp` | 各画面・共通UIヘルパー・What's Newダイアログ・プロジェクトモデル・MIDI/オーディオ入出力の既存カバレッジ |

実行方法(実行ファイル名は`KANADE DAW.exe` — 製品名変更後、`ScoreSmith_artefacts`ディレクトリ名自体は据え置き):

```powershell
& "E:/MIDIDAW/build/ScoreSmith_artefacts/Debug/KANADE DAW.exe" --run-tests
echo $LASTEXITCODE     # 0 = 全通過
# stdoutは持たないGUIサブシステムバイナリのため、結果は同ディレクトリの test-results.txt を読むこと
```

このテストは実際に1件バグを見つけている: イコールパワーパン則が最右端で `std::cos(pi/2)` の浮動小数点誤差により約 -4.4e-8 を返し、無音側チャンネルの符号が反転していた。

---

## GitHub公開について(2026-08-29時点)

**現状: 公開済み。** `https://github.com/nakano-kouki/kanade_DAW` として2026-08-27〜28に初回公開済み。ローカルの公開用ミラー`L:\github公開用\MIDI&DAW\`と`origin/main`のHEADは完全一致(`8498c34`)、working treeもクリーンで、pushし忘れの差分はない。

公開リポジトリは**私用の開発リポジトリ(ここ、`E:\MIDI&DAW`、123コミット)とは完全に別のgit履歴**(GitHub公開ルール通り、履歴を分けて`git init`し直したもの)。公開側の全コミットは以下の6つだけで、私用側の試行錯誤の履歴は一切出ていない:

```
8498c34 Bump version to 0.3.0: add DAWproject import/export   (tag: v0.3.0)
1432519 Bump version to 0.2.0 and add its What's New entry     (tag: v0.2.0)
7cf5a0f Fix VST plugin crash-on-editor bugs; add startup layout and What's New dialog
149afc6 Move exe distribution to GitHub Releases
d2ccbaf Add MIT license
0aa81eb Initial public release of KANADE DAW                   (tag: v0.1.0)
```

exe本体はリポジトリに含めず、GitHub Releasesへ添付する運用(リリースルール通り)。

**訂正(2026-08-31)**: 上記は当初「UTAU連携・ドッキング・セッションビューも未公開」と書いていたが誤り。実際に公開側`v0.1.0`〜`v0.3.0`のツリーを直接確認したところ、UTAU連携Phase1・ドッキングレイアウトシステム・セッションビュー・DAWproject相互運用は**いずれも既に`v0.1.0`の時点で公開済み**だった(私用側で08-26に完成した時点で、直後の初回公開(08-27)に含まれていたため)。`git remote -v`や上位の私用コミット数だけを見て「未公開」と判断せず、実際に公開側のツリー内容を`git ls-tree`等で直接比較しないと機能単位の公開状況は分からない、という教訓が今回さらに積み上がった。

公開ミラーに実際にまだ反映されていない機能は、v0.3.0時点のツリーと私用側HEADを直接diffして確認した以下の4点のみ:

- **拡張プラグインAPI(フォーマット変換)** — 今回のセッションでmainにマージしたばかりの最新機能
- パン数値表示、パフォーマンス設定、出力デバイスのクイック切替

次に公開を更新する際の手順(GitHub公開ルール・リリースルール通り):
1. 私用側`E:\MIDI&DAW`の安定した状態を`L:\github公開用\MIDI&DAW\`へ上書きコピー
2. 公開前チェックリスト(機密情報・個人情報・内部限定情報・著作権・ビルド成果物のgitignore漏れ)を確認
3. `README.md`を公開版にもコピーし、`docs/STATUS.md`は本セクション(GitHub公開について)を除いた公開安全版を別途作成、CHANGELOG.mdに`## [0.4.0]`の追記
4. MINOR相当の新機能4点がまとまっているため、次のタグは`v0.4.0`が妥当(セマンティックバージョニング: MINOR=新機能追加)
5. `git add -A` → `git commit` → `git push` → `git tag v0.4.0` → `git push --tags` → GitHub ReleasesにCHANGELOGの該当箇所を転記、新しい`KANADE DAW.exe`を添付

**未決着の命名問題**: 対外的な製品名・実際に取得したGitHubリポジトリ名はいずれも「KANADE DAW」系(`kanade_DAW`)。一方で、CMakeのプロジェクト名(`ScoreSmith`)、ビルド成果物ディレクトリ名(`ScoreSmith_artefacts`)、ソースコード内のnamespace(`ss::`)、`docs/`配下の設計書・計画書のファイル名や本文、そして**ローカルの公開用ミラーフォルダ名自体(`L:\github公開用\MIDI&DAW\`)**は、依然として旧称「MIDI&DAW」「ScoreSmith」のままになっている。命名・フォルダ構成ルール文書は「正式名称はScoreSmithなのでリポジトリ名は`scoresmith`に揃えるのが自然」と書いているが、実際に確定した対外名称は`kanade_DAW`であり、この文書の記述はもう古い。当面実害はないが、いずれ`L:\github公開用\kanade_DAW\`のようにローカルフォルダ名もリポジトリ名と揃えておくと今後の混乱が減る。

**現在の私用作業フォルダ名`E:\MIDI&DAW`の`&`はビルドツールを壊す既知の問題**(`E:\MIDIDAW`ジャンクション経由の運用は私用側限定の応急処置、詳細は[[../プロジェクトの命名・フォルダ構成について/命名・フォルダ構成ルール|命名・フォルダ構成ルール]])。公開用ミラー側は既にexe/ソースのみのコピーなのでビルド自体は影響を受けないが、フォルダ名の記号は上記の通りいずれ解消したい。
